# Archived SQL files

These files are **superseded** by [`../schema.sql`](../schema.sql).

They were kept for reference only. Do not run them on a new Supabase project — use the single `schema.sql` file instead.

Contents:

- `full_platform.sql` — old combined script (incomplete, included demo seeds)
- `migrations/` — incremental migrations 001–009
- Standalone patches (`admin_worker_permissions.sql`, `fix_reservation_rpc.sql`, etc.)
- `seed.sql`, `seed_trips.sql` — demo data (no longer used)
